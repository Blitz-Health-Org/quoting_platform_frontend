export type ClassType = {
  class_name: string;
  employee: {
    contribution_percentage: number;
    num_lives: number;
  };
  spouse: {
    contribution_percentage: number;
    num_lives: number;
  };
  child: {
    contribution_percentage: number;
    num_lives: number;
  };
  family: {
    contribution_percentage: number;
    num_lives: number;
  };
};
